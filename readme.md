## start
```CMD
docker-compose up --build -d
```

```CMD
docker logs -f mcp-filesystem
docker logs -f mcp-github
docker logs -f mcp-openai
```

install mcp filesystem
```CMD
npm install -g @modelcontextprotocol/server-filesystem
```
